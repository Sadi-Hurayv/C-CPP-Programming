#include<stdio.h>
#include<stdlib.h>
int main()
{
    int NUM;
    char ch[10];
    scanf("%s",&ch,printf("Enter a number (in STRING) : "));
    NUM=atoi(ch);
    printf("The number (in INTIGER) is : %d\n",NUM);
}
